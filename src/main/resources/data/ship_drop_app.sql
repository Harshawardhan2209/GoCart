CREATE DATABASE IF NOT EXISTS SHIP_DROP_APP;
USE SHIP_DROP_APP;

CREATE TABLE INVENTORY (INVENTORY_ID INT NOT NULL AUTO_INCREMENT,TITLE VARCHAR(255) NOT NULL,QUANTITY INT NOT NULL,CATEGORY VARCHAR(255) NOT NULL,PRIMARY KEY (INVENTORY_ID));

INSERT INTO INVENTORY (title, quantity, category) VALUES ("BOOKS", 2, "asff");